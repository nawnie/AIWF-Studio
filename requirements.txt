# torch/torchvision are installed by launch.py with CUDA wheels (cu130).
# torchaudio is not part of the default image runtime because the cu130 wheel
# cadence can lag behind torch/torchvision; set TORCHAUDIO_CUDA_VERSION to opt in.
diffusers>=0.39.0
transformers>=4.44.0,<5.0.0
accelerate>=0.27.0
peft>=0.6.0
safetensors>=0.4.0
gradio>=5.16.0
fastapi>=0.110.0
uvicorn>=0.27.0
pydantic>=2.6.0
pydantic-settings>=2.2.0
Pillow>=10.0.0
qrcode>=7.4.2
numpy>=1.24.0
psutil>=5.9.0
pytest>=8.0.0
spandrel>=0.4.0
spandrel-extra-arches>=0.1.0
opencv-python-headless>=4.8.0
# Bundles a static ffmpeg binary used to transcode generated videos to browser-playable
# H.264 (accessed via imageio_ffmpeg.get_ffmpeg_exe()). A real dependency - not a scavenged path.
imageio-ffmpeg>=0.5.1
facexlib>=0.3.0
compel>=2.0.0
ftfy>=6.1.0
gguf>=0.19.0
# Quantized transformer/text-encoder loaders used by Flux/Z-Image paths.
bitsandbytes>=0.46.0
# SageAttention is optional Wan/video acceleration. It is not listed as a hard
# dependency because Windows CUDA/Triton compatibility changes quickly.
# launch.py may attempt a best-effort install and must fall back cleanly.
# Treat any speed claim as benchmark-gated on the local machine.
# Manual current-upstream test: pip install sageattention==2.2.0 --no-build-isolation
# Opt in during setup: pass --install-sageattention to launch.py
#
# Optional - faster Wan attention when buildable on your CUDA toolchain:
# pip install flash-attn
torchsde>=0.2.6
segment-anything>=1.0
scipy>=1.10.0
kornia>=0.7.0
PyYAML>=6.0.0
tqdm>=4.66.0
sentencepiece>=0.2.0
protobuf>=4.25.0
pypdf>=4.0.0
# ControlNet annotators (depth/openpose/HED/PIDI/MLSD/normal/lineart).
# AIWF imports this lazily; missing annotators degrade to pass-through maps.
controlnet-aux>=0.0.10

# Optional - AMD/Intel GPU on Windows: pip install torch-directml, then
# enable DirectML in Settings -> Launch profile. (Linux AMD: install the
# ROCm torch build instead; it shows up as CUDA automatically.)

# Optional - Face Swap (ReActor). Install to enable the Face Swap tab.
# onnxruntime-gpu for NVIDIA; onnxruntime for CPU-only.
insightface>=0.7.3
onnxruntime>=1.17.0

# Optional audio note: do not add `audiocraft` to the shared Studio venv.
# AudioCraft 1.3.0 pins torch==2.1.0 / torchvision==0.16.0, which conflicts
# with the current CUDA 13 torch stack. AIWF uses Transformers MusicGen here.
